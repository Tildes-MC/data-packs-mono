# Get self head

This is a simple loot table entry that allows for a command that gives someone their own head. Intended to be used in command blocks.

Can be used like this:

- `/execute as @p[distance=..3,limit=1,sort=nearest] run loot give @s loot self_head:get_head`
